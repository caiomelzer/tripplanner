<% 
var lastDate;
var countDay = 1;
for(var i=0; i<items.length; i++) {
	var dateItem = moment(items[i].startAt).get('Y');
	dateItem += moment(items[i].startAt).get('M');
	dateItem += moment(items[i].startAt).get('D');
	if(lastDate && lastDate !== dateItem){ %>
		Day #<%= countDay %> (<%= moment(items[i].startAt).format("MMM Do YY") %>)
		<% countDay++; %>
	<% 
	}else{ 
		if(lastDate === undefined){
	%>
		Day #<%= countDay %> (<%= moment(items[i].startAt).format("MMM Do YY") %>)
		<% countDay++; %>
	<%	}
	 } %>
    <div class="list-group-item item-guide" data-id="<%= items[i]._id %>">
	  <div class="row-action-primary">
	    <i class="material-icons"><%= items[i].category_icon %></i>
	  </div>
	  <div class="row-content">
	    <div class="least-content">
	    	
		</div>
	    <h2 class="list-group-item-heading"><%= items[i].place_name %> (<%= items[i]._id %>)</h2>
		<p class="list-group-item-text text-muted">Duration:
		<%= moment.utc(moment(items[i].startAt).diff(moment(items[i].endAt))).format("HH:mm:ss")%> </p>
		<p class="list-group-item-text text-muted">Cost: 
			<strong class="text-warning">
		    </strong></p>
		</div>
	</div>
	<div class="list-group-separator"></div>
<% lastDate = dateItem; %>
	
<% } %>